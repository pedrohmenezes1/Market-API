version: '3'

services:
  market-api:
    build: .
    image: market-api
    environment:
      - MONGODB_URL=mongodb://mongodb:27017/market-api
    ports:
      - '3000:3000'
    depends_on:
      - mongodb
    volumes:
      - .:/usr/src/market-api
    networks:
      - market-network

  mongodb:
    image: mongo:4.2.1-bionic
    ports:
      - '27017:27017'
    volumes:
      - dbdata:/data/db
    networks:
      - node-network

volumes:
  dbdata:

networks:
  node-network:
    driver: bridge
